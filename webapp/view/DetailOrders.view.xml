<mvc:View controllerName="ejercicio.examen.controller.DetailOrders"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
	xmlns:core="sap.ui.core">
    <Page id="orders" title="{i18n>title}">
        <List headerText="Custom Content" mode="Delete" items="{path: '/Order_Details'}" >
		<CustomListItem>
			<HBox>
				<core:Icon size="2rem" src="sap-icon://attachment-photo" class="sapUiSmallMarginBegin sapUiSmallMarginTopBottom" />
				<VBox class="sapUiSmallMarginBegin sapUiSmallMarginTopBottom" >
					<Link text="Order N°: {OrderID}" press="handlePress"/>
					<Label text="Product ID: {ProductID}"/>
				</VBox>
			</HBox>
		</CustomListItem>
	</List>
    </Page>
</mvc:View>